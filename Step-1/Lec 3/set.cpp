#include<bits/stdc++.h>
using namespace std;
int main(){
    set<int> s;
    s.insert(19);
    s.insert(3);
    s.insert(1);
    s.insert(54);
    s.insert(100);
    s.insert(11);
    int size=s.size();
    for(int i=0; i<size; i++){
        
    }
}
